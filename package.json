{
  "name": "keyman-music-metadata-consultant",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node server.mjs",
    "test": "node --test",
    "check": "node --check app.js && node --check server.mjs && node --check api/contact.js && node --check worker/index.js"
  },
  "engines": {
    "node": ">=20"
  }
}
